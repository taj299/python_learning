import math

# radius = float(input("enter the radius of circle is: "))
# area = math.pi*pow(radius,2)
# print(f"the area of circle is {round(area,2)} cm")

a = float(input("enter side of A : "))
b = float(input("enter the side of B: "))

c = math.sqrt(pow(a,2) + pow(b,2))
print(f"side c = {c} ")
